function solve(first, second, delimiter){
    let firstResult = first + delimiter + second;
   // let seondResult = `${first}${delimeter}${second}`;
   console.log(firstResult);    
}

solve('John', 'Smith', '->');